ktor:
  application:
    modules:
      - com.taskconvertai.ApplicationKt.module
  deployment:
    port: 8082
postgres:
  url: "jdbc:postgresql://localhost:5432/analyzer"
  user: "analyzer_user"
  password: "analyzer_pass"
ai:
  baseUrl: "http://localhost:8000"
  requestTimeoutMs: 1500000 # 25 минут
cleanup:
  jobMaxAgeHours: 24 # Удалять задачи старше 24 часов
  cleanupIntervalHours: 6 # Запускать очистку каждые 6 часов
